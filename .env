# --- OpenAI ---
OPENAI_API_KEY=
# --- GCP / BigQuery ---
GCP_PROJECT_ID=
BQ_DATASET_ID=faq_bot
BQ_TABLE_ID=faq_embeddings
BQ_QUERY_LOG_TABLE_ID=query_logs

# Path to your service account key JSON (see terraform/ for how to get one).
# Omit this if you're using Application Default Credentials instead
# (e.g. `gcloud auth application-default login`, or running on a GCE VM
# with an attached service account).
GOOGLE_APPLICATION_CREDENTIALS=faq-bot-key.json

# --- WhatsApp bot (whatsapp_bot.py) ---
# Where Neonize stores the WhatsApp session. In production, point this at
# the persistent disk Terraform attaches (see terraform/main.tf):
#   NEONIZE_DB_PATH=/mnt/bot-data/neonize.db
NEONIZE_DB_PATH=./neonize.db
